name: WeMakePythonStyleGuide

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: wemake-python-styleguide
      uses: wemake-services/wemake-python-styleguide@1.5.0
